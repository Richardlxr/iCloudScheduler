# 启动、提醒与草稿补全验证

日期：2026-09-15。仅本地修改及验证，未发布、未替换已安装应用。

## 检查结果

- `./scripts/build-app.sh debug arm64`：通过，构建本地 ad-hoc `.app`。
- `SchedulerChecks`：81 通过、0 失败。包含只设开始时间、无效日期、错误结束时间、全天缺结束日期、菜单栏配置及旧配置兼容。
- `iCloudScheduler --self-check`：26 通过、0 失败。包含登录 Apple Event 与手动启动事件的分支。
- `iCloudScheduler --workflow-check`：77 通过、0 失败。包含补全成功、失败、取消、返回多项、仍缺信息、无草稿继续生成、清空后过时响应；不访问网络、钥匙串或真实日历。
- `bash -n` 和 `git diff --check`：通过。
- `package-release.sh`：当前 ad-hoc 包按预期被正式分发签名检查拦截。

## 原生界面检查

使用不同 Bundle ID 的隔离测试包和注入的模型/日历实现：

- 草稿内展示自然语言补全入口。
- 点击“明天”和“下午3点”，输入合成为“明天，下午3点”，AI 补全按钮启用。
- 点击补全后仍显示草稿确认，未写入真实日历。
- 手动编辑显示原生开始/结束日期时间选择器。
- 取消“设置结束时间”后显示时间点提醒说明；保存后卡片显示“2035-09-14 20:00 · 时间点提醒”。
- ⌘ , 能打开设置。切换通用页时 computer-use 原生连接断开，重置后仍不可用；菜单栏图标开关的实际点击验收未完成。

截图：`dist/validation/review-completion.png`。

## 尚未验证的系统行为

- 真正注销/登录后的静默启动（已覆盖启动事件识别分支）。
- 真实 EventKit 写入、系统通知送达与跨设备同步。
- 正式 Developer ID 版本之间的权限连续性。本机只有 Apple Development 签名身份；旧 ad-hoc 权限不能由应用强行迁移。
- 当前真实模型对新提示词及补全语句的遵循程度；本次使用注入模型进行回归。

现有编译器警告为 Keychain 常量弃用和 Command Line Tools 链接搜索目录缺失，未导致构建或离线检查失败。
